#!/bin/sh
# This file closes down the tap device.

ip route del <%= @ip_address.split('.')[0..2].join('.') %>.0/24 dev $INTERFACE
ip addr del <%= @ip_address %>/32 dev $INTERFACE
ip link set $INTERFACE down
